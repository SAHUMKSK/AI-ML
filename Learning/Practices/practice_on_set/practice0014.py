# 14. Check whether {1,2} and {3, 4} are disjoint.

setA = {1, 2}
setB = {3, 4}

if setA.isdisjoint(setB):
    print("Set A and Set B are disjoint.")
else:
    print("Set A and Set B are not disjoint.")